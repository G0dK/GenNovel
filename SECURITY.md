# 安全策略

## 支持版本

GenNovel 当前仅为最新发布版本和 `main` 分支提供安全更新。旧版本用户应先升级到最新版本，再确认
问题是否仍然存在。

## 报告安全问题

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口：

https://github.com/G0dK/GenNovel/security/advisories/new

报告中请包含：

- 受影响版本、提交或部署方式；
- 可复现步骤和最小复现样例；
- 实际影响与可能的攻击路径；
- 已验证的缓解措施（如有）。

请勿在公开 Issue 中提交未修复漏洞、访问令牌、模型密钥、真实书籍数据库或服务日志中的敏感信息。

## 响应流程

维护者确认报告后会进行复现、影响评估和修复安排。修复完成后，将视影响范围发布安全更新并在
Changelog 或 GitHub Security Advisory 中说明受影响版本及升级建议。

## 部署提示

- 面向非本机地址运行 Web 服务时设置强随机 `GENNOVEL_TOKEN`。
- 使用 HTTPS 反向代理并限制管理接口的网络访问范围。
- 不要将 `.env`、书籍数据库、备份文件和运行日志提交到版本库。
- 为模型服务密钥配置最小权限、额度限制和定期轮换策略。
